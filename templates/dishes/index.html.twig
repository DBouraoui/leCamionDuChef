{% extends 'base.html.twig' %}

{% block title %}Menu du chef{% endblock %}

{% block body %}
    <section id="menu" class="relative py-16 sm:py-24 bg-gradient-to-br from-lime-50 to-white">
        <div class="container mx-auto px-4">
            <!-- En-tête du Food Truck -->
            <div class="text-center mb-12">
                <img src='{{ asset('pictures/Logo.svg') }}' alt="Logo du Food Truck" class="h-32 w-32 mx-auto mb-4 rounded-full object-cover border-4 border-lime-500 shadow-lg">
                <h1 class="text-4xl font-bold text-lime-800 mt-6">{{ foodtruck.name|default('Le Food Nomade') }}</h1>
                <p class="text-gray-600 mt-2 max-w-2xl mx-auto">{{ foodtruck.description|default('Cuisine de rue gourmande et authentique. Des saveurs du monde entier dans votre quartier.') }}</p>
            </div>

            <!-- Notre Menu -->
            <h2 class="text-2xl font-bold text-lime-800 mb-6 text-center">Notre Menu</h2>

            <!-- Liste des plats -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                {% for dish in menu|default([
                    {id: 1, name: 'Burger Classic', description: 'Bœuf, cheddar, salade, tomate, oignon rouge, sauce maison', price: 8.50, image: '/api/placeholder/300/200'},
                    {id: 2, name: 'Burger Veggie', description: 'Steak de légumes, avocat, roquette, tomate séchée, sauce yaourt aux herbes', price: 9.00, image: '/api/placeholder/300/200'},
                    {id: 3, name: 'Frites Maison', description: 'Pommes de terre fraîches, sel de mer, herbes de Provence', price: 3.50, image: '/api/placeholder/300/200'},
                    {id: 4, name: 'Brownie', description: 'Chocolat noir, noix, servi tiède', price: 4.00, image: '/api/placeholder/300/200'},
                    {id: 5, name: 'Limonade Artisanale', description: 'Citrons frais, menthe, sucre de canne', price: 3.00, image: '/api/placeholder/300/200'}
                ]) %}
                    <div class="bg-white rounded-lg overflow-hidden shadow-md hover:shadow-lg transition-shadow">
                        <img src="{{ asset('uploads/dishes/' ~ dish.pictureUrl) }}" alt="{{ dish.title }}" class="w-full h-64 object-cover">
                        <div class="p-4">
                            <div class="flex justify-between items-start">
                                <h3 class="text-lg font-bold text-lime-800">{{ dish.title }}</h3>
                                <span class="bg-lime-100 text-lime-800 font-bold px-2 py-1 rounded-full">{{ dish.price|number_format(2, ',', ' ') }} €</span>
                            </div>
                            <p class="text-gray-600 mt-2">{{ dish.description }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>

            <!-- Informations de contact et localisation -->
            <div class="mt-16 text-center">
                <h2 class="text-2xl font-bold text-lime-800 mb-4">Où nous trouver ?</h2>
                <div class="bg-white rounded-lg shadow-md p-6 max-w-xl mx-auto">
                    <p class="text-gray-700 mb-4">
                        Consultez nos réseaux sociaux pour connaître nos emplacements du jour !
                    </p>
                    <div class="flex justify-center items-center gap-4">
                        <div class="text-center">
                            <p class="font-bold text-gray-800">Contact</p>
                            <p class="text-gray-600">📱 06 12 34 56 78</p>
                            <p class="text-gray-600">📧 contact@foodtruck.fr</p>
                        </div>
                        <div class="text-center">
                            <p class="font-bold text-gray-800">Horaires</p>
                            <p class="text-gray-600">Du lundi au vendredi</p>
                            <p class="text-gray-600">11h30 - 14h00 | 18h00 - 21h30</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}